apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: message-demo
  template:
    metadata:
      labels:
        component: message-demo
    spec:
      containers:
        - name: client
          image: dashboard
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: BACKEND_API
              value: 'api.message.com'
